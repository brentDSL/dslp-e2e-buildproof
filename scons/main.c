#include <stdio.h>

/* Generated by the SConstruct from the flavour SCons was given, so an archive
   carrying a program built with the WRONG flavour fails the journey's
   assertion instead of passing it silently. The flavour reaches the
   SConstruct from the build option's scons_args and from nowhere else. */
#include "dslp_flavour.h"

int main(void) {
    printf("%s %s\n", "dslp sconsproof ok", DSLP_FLAVOUR);
    return 0;
}
// webhook-build push f8fc92d99d1ace7f
// webhook-build push 0b60ad1696b4c553
// webhook-build push 58b89f8ec362d958
// webhook-build push 2b523508d2628f58
// webhook-build push c7b59f1657b7f802
// webhook-build push 4140910156a561e6
// webhook-build push abd1c470c45a1654
// webhook-build push 6c92d6abd7d00e16
// webhook-build push 366ba434cef3cc56
// webhook-build push 424ea7d31ca53b21
// webhook-build push 4373cb2504bc342d
