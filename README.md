# dslp-e2e-buildproof

Synthesized fixture for the DSL platform's customer-perspective build e2e.
No real project data — a minimal CMake target the build plane compiles to
prove the push -> build -> green loop.
